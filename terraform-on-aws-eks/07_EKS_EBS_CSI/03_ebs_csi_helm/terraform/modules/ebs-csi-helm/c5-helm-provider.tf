# Helm and Kubernetes providers are configured in env-dev/providers.tf
# using the cluster endpoint and token from aws_eks_cluster data source.
# No additional provider config needed at module level.
