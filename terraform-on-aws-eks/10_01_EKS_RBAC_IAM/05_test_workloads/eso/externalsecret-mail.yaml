apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pulseauth-mail-external-secret
  namespace: backend-prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: pulseauth-secret-store
    kind: SecretStore
  target:
    name: pulseauth-mail-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: eks-rbac-dev/pulseauth/mail
