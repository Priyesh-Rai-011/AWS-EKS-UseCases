apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels: 
    app: backend-restapp
    tier: backend

spec:
  # type: ClusterIP   ---> no need to mention it : Cluster IP is default
  selector:
    app: myapp
  ports:
  - port: 8080
    targetPort: 8080