apiVersion: v1
kind: Service
metadata:
  name: my-backend-service
  labels: 
    app: backend-restapp
    tier: backend

spec:
  # type: ClusterIP   ---> no need to mention it : Cluster IP is default
  selector:
    app: backend-restapp
  ports:
  - port: 8080
    targetPort: 8080